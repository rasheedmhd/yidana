<nav class="p-3 text-bg-dark sticky-top">
  <div class="container-fluid">
    <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-start">
      <a href="<%= root_path %>" class="d-flex align-items-center mb-2 mb-md-0 text-white text-decoration-none">
        <svg class="bi me-2" width="40" height="32" role="img" aria-label="Bootstrap"><use xlink:href="#bootstrap"/></svg>
        X
      </a>

      <ul class="nav col-12 col-md-auto mx-md-auto mb-2 justify-content-center mb-md-0">
        <li><%= link_to "Home", root_path, class: "nav-link px-2 text-secondary" %></li>
        <li><a href="#" class="nav-link px-2 text-white">Features</a></li>
        <li><a href="#" class="nav-link px-2 text-white">Pricing</a></li>
        <li><a href="#" class="nav-link px-2 text-white">FAQs</a></li>
        <li><a href="#" class="nav-link px-2 text-white">About</a></li>
      </ul>

      <% if rodauth(:user).logged_in? %>
      <div class="nav me-md-3">
        <%= link_to "Dashboard", dashboard_path, class: "px-2 btn btn-sm btn-outline-light me-2", data: { turbo: false } %>
      </div>
      <%= render 'shared/nav_user' %>
      <% else %>
      <%= link_to "Login", rodauth(:user).login_path, class: "btn btn-outline-light me-2" %>
      <%= link_to "Register", rodauth(:user).create_account_path, class: "btn btn-warning" %>
      <% end %>
    </div>
  </div>
</nav>
