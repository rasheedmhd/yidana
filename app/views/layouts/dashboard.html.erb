<!DOCTYPE html>
<html lang="en" data-bs-theme="auto">
  <head>
    <title><%= app_name %><%= " - #{@page_title}" if @page_title %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body class="bg-body-tertiary">
    <%= render "messages" %>
    <main class="row">
      <div class="col-md-2">
        <%= render 'sidebar' %>
      </div>
      <div class="col-md-10">
        <div class="p-1" style="height: 65px">
          <%# Fake padding for now %>
        </div>
        <%= yield %>
      </div>
    </main>
    <%= render 'color_modes' %>
  </body>
</html>
