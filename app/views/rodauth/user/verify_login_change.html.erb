<%= form_with url: rodauth(:user).verify_login_change_path, method: :post, data: { turbo: false } do |form| %>
  <div class="form-group mb-3">
    <%= form.submit rodauth(:user).verify_login_change_button, class: "btn btn-lg btn-primary w-100 fs-6" %>
  </div>
<% end %>
