<% unless rodauth(:user).login_form_footer_links.empty? %>
  <%== rodauth(:user).login_form_footer_links_heading %>

  <ul>
    <% rodauth(:user).login_form_footer_links.sort.each do |_, link, text| %>
      <li><%= link_to text, link %></li>
    <% end %>
  </ul>
<% end %>
