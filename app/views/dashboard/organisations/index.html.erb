<% content_for :toolbar do %>
  <%= render partial: 'toolbar', locals: { resource_class: Organisation, actions: 'toolbar_actions_multiple'} %>
<% end  %>

<%= render partial: "table", locals: { resource_class: Organisation,
                                       resources: @organisations, pagy: @pagy,
                                       fields: Organisation.columns.map(&:name).map(&:to_sym) } %>
