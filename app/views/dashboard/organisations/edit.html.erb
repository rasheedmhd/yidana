<% content_for :toolbar do %>
  <%= render partial: 'toolbar',
              locals: { resource_class: Organisation, resource: @organisation, actions: 'toolbar_actions_single'} %>
<% end %>

<turbo-frame id="<%= dom_id @organisation %>" data-turbo-action="advance">
  <%= render "form", organisation: @organisation %>
</turbo-frame>
